{
  "name": "label-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "selftest": "node --env-file-if-exists=.env scripts/selftest-storage.js",
    "run-local": "node --env-file-if-exists=.env scripts/run-local.js",
    "import-translations": "node --env-file-if-exists=.env scripts/import-translation-db.js",
    "setup-storage": "node --env-file-if-exists=.env scripts/setup-storage.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "exceljs": "^4.4.0",
    "express": "^4.21.2",
    "jszip": "^3.10.1",
    "multer": "^2.0.2",
    "xlsx": "^0.18.5"
  }
}
